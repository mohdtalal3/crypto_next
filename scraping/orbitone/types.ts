import type { JsonObject } from "@/types";

export interface OrbitSyncData {
  overview?: JsonObject;
  exAiStatistics?: JsonObject;
  investments?: JsonObject;
  partners?: JsonObject;
  partnerReferrals?: JsonObject;
  rankStatistics?: JsonObject;
}
